# a module is a file containing python definitions and statement or function.
#we can make our own function and import it as a module.
#for example here we have make a function name example_of_module.py
from example_of_module import fib
x=fib(9)
print(x)
print(dir(fib))